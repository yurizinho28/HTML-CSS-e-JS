let userName = document.querySelector('.userName');
let senha = document.querySelector('.senha');
let login = document.querySelector('.login');

login.addEventListener('submit', function(enviar) {
    enviar.preventDefault();
})